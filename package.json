{
  "name": "docker-nodejs-hello",
  "private": true,
  "version": "0.0.1",
  "description": "Node.js Hello world app using nodeJS docker image",
  "dependencies": {
    "express": "3.2.4"
  },
  "scripts": {
    "prestart" : "npm install",
    "start" : "node index.js"
  }
}